#include <iostream>
using namespace std;

int main() {
    int height = 3;
    int width = 2;
    
    if (height > width) {
        cout << "The height is greater than the width." << endl;
    } else {
        cout << "The height is not greater than the width." << endl;
    }
    
    return 0;
}
